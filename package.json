{
  "name": "codeless",
  "displayName": "Codeless",
  "description": "Process files without writing code",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:codelessExplorer"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "codeless-explorer": [
        {
          "type": "webview",
          "id": "codelessExplorer",
          "name": "Codeless Explorer"
        }
      ]
    },
    "commands": [
      {
        "command": "codeless.updateInstruction",
        "title": "Update Instruction"
      },
      {
        "command": "codeless.processFiles",
        "title": "Process Files",
        "category": "Codeless"
      },
      {
        "command": "codeless.refreshFileList",
        "title": "Refresh File List",
        "category": "Codeless"
      },
      {
        "command": "codeless.selectFile",
        "title": "Select/Deselect File"
      },
      {
        "command": "codeless.toggleFile",
        "title": "Toggle File Selection"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "codeless.refreshFileList",
          "when": "view == codelessExplorer",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "^14.x.x",
    "@types/vscode": "^1.60.0",
    "@typescript-eslint/eslint-plugin": "^4.x.x",
    "@typescript-eslint/parser": "^4.x.x",
    "eslint": "^7.x.x",
    "typescript": "^4.x.x"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "openai": "^4.52.7"
  }
}
